.title
  Listado de Clientes
%hr
= link_to customers_path(), class: 'btn btn-info' do
  %i.fa.fa-edit
  Clientes Activos
%br
%br
%table.table
  %th Nombre
  %th Teléfono de Contacto
  %th Correo Electrónico
  %th
    - @customers.each do |customer|
      %tr
        %td
          = link_to customer.name, customer
        %td
          = customer.phone
        %td
          = customer.email
        %td
          = link_to 'Activar', edit_customer_path(customer, origin: "ACTIVATE"), class: 'btn btn-raised btn-success'
          = link_to customer, method: :delete, data: { confirm: '¿Estás seguro que quieres eliminar este cliente?' }, class: 'btn btn-raised btn-danger' do
            %i.fa.fa-trash-o
            Eliminar
