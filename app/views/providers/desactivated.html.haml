.title
  Listado de Proveedores
  .title_label
    .label.label-warning
      Desactivados
%hr

= link_to providers_path(), class: 'btn btn-info' do
  %i.fa.fa-edit
  Proveedores Activos

%br
%br
%table.table
  %th Nombre
  %th Teléfono de contacto
  %th Correo Electrónico
  %th
    - @providers.each do |provider|
      %tr
        %td
          = link_to provider.name, provider
        %td
          = provider.phone
        %td
          = provider.email
        %td
          = link_to 'Activar', edit_provider_path(provider, origin: "ACTIVATE"), class: 'btn btn-raised btn-success'
          = link_to provider, method: :delete, data: { confirm: '¿Estás seguro que quieres eliminar este proveedor?' }, class: 'btn btn-raised btn-danger' do
            %i.fa.fa-trash-o
            Eliminar
