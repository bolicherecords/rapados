.title
  Stock de Productos
%hr
= link_to new_product_path, class: 'btn btn-raised btn-success' do
  %i.fa.fa-plus-circle
  Nuevo Producto
= link_to desactivated_products_path(), class: 'btn btn-info' do
  %i.fa.fa-edit
  Productos Desactivados
%br
%br
%table.table
  %th Producto
  %th Tienda
  %th Cantidad
  %th Fecha Creación
  %th
    - @stocks.each do |stock|
      %tr
        %td
          = link_to stock.product.name, stock.product
        %td
          = link_to stock.store.name, stock.store
        %td
          = stock.amount
        %td
          = stock.created_at
        %td
          = link_to 'Editar', edit_stock_path(stock), class: 'btn btn-raised btn-primary'
          = link_to stock, method: :delete, data: { confirm: '¿Estás seguro que quieres eliminar este producto?' }, class: 'btn btn-raised btn-danger' do
            %i.fa.fa-trash-o
            Eliminar